export type SignUpUserInput = {
    email: string,
    password: string
}
export type LoginUserInput = {
    email: string,
    password: string
};

export type ArticleCardProps = {
    imageUrl: string;
    title: string;
    category: string;
    date: string;
    author: string;

}
